<!DOCTYPE html>
<html>
  <head>
    <title>47 Collection</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <script src="https://kit.fontawesome.com/baa2ae3dc1.js" crossorigin="anonymous"></script>
  </head>

  
  <body class="bg-base-200 min-h-screen flex flex-col">
    <!-- ナビバー -->
    <div class="navbar bg-base-100 shadow">     
      <div class="flex-1">
        <% if user_signed_in? %>
          <%= link_to "47コレクション", posts_path, class: "btn btn-ghost normal-case text-xl" %>
        <% else %>
          <%= link_to "47コレクション", root_path, class: "btn btn-ghost normal-case text-xl" %>
        <% end %>
      </div>

      <div class="flex-none">
        <!-- PCメニュー -->
        <ul class="menu menu-horizontal px-1 hidden md:flex">
          <% if user_signed_in? %>
            <li><%= link_to "みんなの投稿", posts_path %></li>
            <li><%= link_to "マイコレクション", my_posts_index_path %></li>
            <li><%= link_to "いいね一覧", liked_posts_posts_path %></li>
            <li><%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete } %></li>
          <% else %>
            <li><%= link_to "みんなの投稿", posts_path %></li>
            <li><%= link_to "ログイン", new_user_session_path %></li>
            <li><%= link_to "新規登録", new_user_registration_path %></li>
          <% end %>
        </ul>

        <!-- モバイルメニューボタン -->
        <div class="md:hidden dropdown dropdown-end">
          <button class="btn btn-square btn-ghost">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="inline-block h-5 w-5 stroke-current"> <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path> </svg>
          </button>
          <ul tabindex="0" class="menu dropdown-content bg-base-100 rounded-box z-1 mt-4 w-52 p-2 shadow-sm">
            <% if user_signed_in? %>
              <li><%= link_to "みんなの投稿", posts_path %></li>
              <li><%= link_to "マイコレクション", my_posts_index_path %></li>
              <li><%= link_to "いいね一覧", liked_posts_posts_path %></li>
              <li><%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete } %></li>
            <% else %>
              <li><%= link_to "みんなの投稿", posts_path %></li>
              <li><%= link_to "ログイン", new_user_session_path %></li>
              <li><%= link_to "新規登録", new_user_registration_path %></li>
            <% end %>
          </ul>

        </div>

      </div>
    </div>

    <!-- フラッシュメッセージ -->
    <div class="container mx-auto mt-4 px-4">
      <% if notice %>
        <div class="alert alert-success shadow-lg mb-2"><%= notice %></div>
      <% end %>
      <% if alert %>
        <div class="alert alert-error shadow-lg mb-2"><%= alert %></div>
      <% end %>
    </div>

    <!-- コンテンツ -->
    <main class="flex-grow container mx-auto px-4 mt-6">
      <%= yield %>
    </main>

    <!-- フッター -->
    <footer class="footer footer-center p-4 bg-base-300 text-base-content">
      <aside>
        <p>© 2025 47コレクション - All rights reserved</p>
      </aside>
    </footer>
  </body>
</html>

